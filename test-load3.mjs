import puppeteer from "puppeteer"; (async () => { const browser = await puppeteer.launch({args: ["--no-sandbox"]}); const page = await browser.newPage(); page.on("console", m => console.log("LOG:", m.text())); page.on("pageerror", e => console.log("PAGE ERROR:", e)); page.on("error", e => console.log("ERROR:", e)); await page.goto("http://localhost:3000"); await new Promise(r => setTimeout(r, 2000)); console.log("Body content:", await page.content()); await browser.close(); })();